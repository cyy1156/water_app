# 主包 GLB 模型（供 xr-frame 使用）

xr-frame 在部分环境下从**主包**加载模型更稳定。请将智能体 GLB 放在此目录并命名为：

**`agent_default.glb`**

- 路径：项目根目录下 `models/agent_default.glb`
- 首页 3D 组件会从 `/models/agent_default.glb` 加载。

若你已有 `packageModels/glb/agent_default.glb`，复制一份到本目录即可。
